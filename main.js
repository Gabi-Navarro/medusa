
// Inicializar o banner usando slick.js
$(document).ready(function(){
    $('.banner').slick({
        dots: true,
        infinite: true,
        speed: 300,
        slidesToShow: 1
    });
});

// Código para header fixo usando topo-scroll.js já foi incluído no HTML através do CDN.
